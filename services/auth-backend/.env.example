# Server
PORT=3700
NODE_ENV=development

# Auth mode: 'mock' for testing without real OAuth, 'real' for production
AUTH_MODE=mock

# Frontend origin for CORS and redirects
FRONTEND_ORIGIN=http://localhost:8080

# Database
DATABASE_URL=postgres://massivoto:massivoto_dev@localhost:5432/massivoto

# GitHub OAuth (required in real mode)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost:3700/oauth/github/callback

# Google OAuth (required in real mode)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3700/oauth/google/callback
