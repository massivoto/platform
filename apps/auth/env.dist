# User ID for local development (no Google login required)
VITE_USER_ID=nicolas@robusta.build

# Optional: Backend URL (defaults to http://localhost:4000)
# VITE_BACKEND_URL=http://localhost:4000

# Optional: Google OAuth (only needed if you want Google login)
# VITE_GOOGLE_CLIENT_ID=your-google-client-id
