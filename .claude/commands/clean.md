# Clean

Clean build artifacts and temporary files.

## Commands

- `yarn clean` - Remove all dist folders and node_modules
- `yarn clean:claude` - Remove Claude temp files (tmpclaude-*)
- `yarn clean:install` - Clean and reinstall dependencies

## Process

1. Determine what needs cleaning
2. Run appropriate clean command
3. Confirm cleanup completed
